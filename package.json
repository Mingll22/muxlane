{
  "name": "muxlane",
  "version": "0.1.0",
  "private": true,
  "description": "Muxlane monorepo quality commands",
  "packageManager": "pnpm@10.16.1",
  "engines": {
    "node": ">=22.22.2 <23",
    "pnpm": ">=10.16.1 <11"
  },
  "scripts": {
    "format": "pnpm exec prettier --write .",
    "format:check": "pnpm exec prettier --check .",
    "lint": "pnpm --filter muxlane-desktop lint",
    "typecheck": "pnpm --filter muxlane-desktop typecheck",
    "test": "pnpm --filter muxlane-desktop test",
    "build": "pnpm --filter muxlane-desktop build",
    "verify": "pnpm format:check && pnpm lint && pnpm typecheck && pnpm test && pnpm build && cargo fmt --all --check && cargo check --workspace --exclude muxlane-desktop --all-targets && cargo clippy --workspace --exclude muxlane-desktop --all-targets --all-features -- -D warnings && cargo test --workspace --exclude muxlane-desktop --all-features",
    "verify:desktop": "cargo check --manifest-path apps/desktop/src-tauri/Cargo.toml"
  },
  "devDependencies": {
    "prettier": "3.9.5"
  }
}
