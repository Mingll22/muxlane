{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Muxlane grants only Tauri core events and the explicitly registered formal runtime commands; no shell, filesystem, process, or network plugin is permitted.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
