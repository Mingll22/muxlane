{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Phase 3 grants only Tauri core events and window basics. The custom command surface is registered explicitly; no shell, filesystem, process, or network plugin is permitted.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
