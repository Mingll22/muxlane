{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Phase 0 desktop shell has no plugin, filesystem, shell, process, network, or custom command permissions.",
  "windows": ["main"],
  "permissions": []
}
